# ==================================================
# Wyly Local Development Environment Variables
# ==================================================
# このファイルを .env にコピーして使用してください
# cp .env.sample .env

# --------------------------------------------------
# PostgreSQL
# --------------------------------------------------
POSTGRES_USER=wyly
POSTGRES_PASSWORD=wyly_dev_password
POSTGRES_DB=wyly

# --------------------------------------------------
# app-backend
# --------------------------------------------------
PORT=8080
# ホスト側のポート（docker-compose.yamlで25432, 28080にマッピング）
# DATABASE_URLはdocker-compose.yaml内で自動設定されます
# CORS許可オリジン（カンマ区切りで複数指定可能）
CORS_ALLOWED_ORIGINS=http://localhost:23000
